I have built a website using chatgpt and I want to continue that chat from here  so I am providing a rough Idea about that chat 

🧠 Summary — “C Programs / Study Tracker” React + Firebase App
⚙️ Tech Stack

React (Vite or CRA)

Firebase (Firestore + Auth)

TailwindCSS for styling

react-syntax-highlighter for C code syntax highlighting

react-feather icons for UI

🎯 Project Overview

A C Programming Study Tracker web app for organizing, solving, and tracking coding questions.
Users can:

View categorized programming questions

Add new questions with optional code

Mark them complete

Search and filter questions

Switch between light and dark modes

Admin users can additionally:

Add/delete categories

Edit/delete questions

Manage all content directly from the UI

⚙️ Firebase Configuration
Firebase Project:

Name: study-tracker (previously codesmanager)

Configuration (firebaseConfig):
const firebaseConfig = {
  apiKey: "AIzaSyBJKqkr4ltuPMzAbolCbH8u4WdPQRMMMS4",
  authDomain: "codesmanager.firebaseapp.com",
  projectId: "codesmanager",
  storageBucket: "codesmanager.firebasestorage.app",
  messagingSenderId: "821213605605",
  appId: "1:821213605605:web:b778fae8ed7dcf3972ae0c",
  measurementId: "G-LV8LFGF7QP"
};

Initialization Code:
import { initializeApp } from "firebase/app";
import { getFirestore } from "firebase/firestore";
import { getAuth } from "firebase/auth";

const app = initializeApp(firebaseConfig);
export const db = getFirestore(app);
export const auth = getAuth(app);

🔑 Authentication

Admin email: mshoaibkhan0988@gmail.com

Admin-only actions:

Edit questions

Delete questions

Delete categories

Normal users:

Can add new questions (visible to everyone)

Cannot edit or delete existing questions

Can mark questions complete

Can search and browse freely

Login handled through Firebase Auth with Email/Password.

🗂 Firestore Data Structure

Collection: questions
Each document structure:

{
  text: "Write a C program to reverse a string",
  category: "Strings",
  code: "C code here",
  complete: false
}

🧱 Core App Features
🗃 Categories

Default categories:

[
  "Strings",
  "1D arrays",
  "2D arrays",
  "Functions",
  "Recursion",
  "Pointers",
  "Structures",
  "File handling",
  "Loops & Patterns"
]


Admin can add/delete categories.

Clicking a category scrolls smoothly to its section.

📝 Questions

Each question shows:

Text

Completion checkbox

Edit (admin only)

Delete (admin only)

Clicking a question toggles code visibility.

Code blocks are syntax highlighted (using Prism )